【数据来源】
源数据格式为.json，共59000组数据，选择另存为txt。
【数据处理】
先逐行读取txt，然后使每一行对应dataframe里的一个单元格，合成整个的dataframe，然后根据标点符号和原数据行与行之间的规律剔除冗余行。
然后在疑问多答的情况下保留每个问题的第一个回答，生成新的dataframe，共四个属性：question，description，keywords，answer_text。
最终将59000个回答（存在一问多答情况）筛选至22341组数据（一问一答），并导出为csv格式，供后续模型使用。